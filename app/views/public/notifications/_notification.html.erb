<% visitor_id = notification.visitor_id %>
<% visited_id = notification.visited_id %>

<div class='m-2 table-back'>
  <%= notification.visitor.teacher_name %>
  先生が
  <%= link_to 'あなたにメッセージ', room_path(notification.room_id), class: "border-bottom" %>
  を送りました。

  <span><%= " (#{time_ago_in_words(notification.created_at)}前)" %></span><br>
</div>