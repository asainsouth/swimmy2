/*.messageを部分更新。appendメソッドで要素を追加 */
$('#area').append("<div class ='talk_right clearfix'><p><%=@name%><%= @chat.message %><br></p></div>");

/*チャット投稿後、フォームの値に前回の投稿内容が残らないようにする */
$("#chat_message").val("");

chatScroll();
